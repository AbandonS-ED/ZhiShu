# 项目级 npm 配置 — 全队自动生效
# 国内网络环境下使用 npmmirror（中国大陆），无需 VPN 即可顺畅装包
registry=https://registry.npmmirror.com

# 关闭噪音输出，install 更快更安静
fund=false
audit=false

# 已知 Next 14 + eslint 8 的几个 deprecation warning 不影响 build，跳过严格检查
# （如需关掉可在此处加 strict-peer-deps 等）
