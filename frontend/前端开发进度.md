# 前端开发进度

> 最后更新：2026-06-11（管理后台 9 页面 + 批量删除 + 布局隔离）

## 总览

| 模块 | 状态 | 完成度 |
|------|------|--------|
| 学生端 8 页面模板复刻 | ✅ 已完成 | 100% |
| 学生端 8 页面后端联调 | ✅ 已完成 | 100% |
| **管理后台 9 页面** | **✅ 已完成** | **100%（含批量删除）** |
| 共享组件（Sidebar / Header） | ✅ 已完成 | 100% |
| 共享状态（AdminProvider） | ✅ 已完成 | 100% |
| 共享组件（AdminCheckbox / BatchDeleteBar / useSelection） | ✅ 已完成 | 100% |
| API 客户端（api.ts，9 模块 + 自动带 token） | ✅ 已完成 | 100% |
| 401 自动跳登录页 | ✅ 已完成 | 100% |
| 登录注册（JWT） | ✅ 已完成 | 100% |
| Next.js build 17 路由 | ✅ 编译通过 | 100% |
| Lint 0 errors | ✅ 通过 | 100% |

---

## 已完成的学生端页面

| 路由 | 页面 | 模板源 | 后端联调 | 状态 |
|------|------|--------|----------|------|
| `/` | 仪表盘 | `yibiaopan.html` | ✅ 真实数据聚合 | ✅ |
| `/login` | 登录/注册 | `login.html` | ✅ JWT 认证 | ✅ |
| `/duihua` | 智能对话 | `duihua.html` | ✅ SSE 流式 | ✅ |
| `/profile` | 学习画像 | `huaxiang.html` | ✅ AI 弹窗 | ✅ |
| `/resources` | 资源中心 | `zhiyuan.html` | ✅ SSE 流式 | ✅ |
| `/path` | 学习路径 | `lujing.html` | ✅ SSE 流式 | ✅ |
| `/tiku` | 练习题库 | `tiku.html` | ✅ SSE 流式 (dual-format) | ✅ |
| `/pinggu` | 学习评估 | `pinggu.html` | ✅ AI 评估 | ✅ |

---

## 已完成的管理后台页面

| 路由 | 页面 | 模板源 | 后端联调 | 状态 |
|------|------|--------|----------|------|
| `/admin/login` | 管理员登录 | `houtai.html` 登录卡片 | ✅ /auth/login + role 校验 | ✅ |
| `/admin` | 仪表盘 | houtai.html 仪表盘 | ⚠️ 硬编码（待后端） | ✅ |
| `/admin/users` | 用户管理 | houtai.html 表格 | ⚠️ 硬编码 + **批量删除** | ✅ |
| `/admin/resources` | 资源管理 | houtai.html 表格 | ⚠️ 硬编码 + **批量删除** | ✅ |
| `/admin/exercises` | 练习题 | houtai.html 表格 | ⚠️ 硬编码 + **批量删除** | ✅ |
| `/admin/paths` | 学习路径 | houtai.html 表格 | ⚠️ 硬编码 + **批量删除** | ✅ |
| `/admin/chats` | 对话记录 | houtai.html 表格 | ⚠️ 硬编码 + **批量删除** | ✅ |
| `/admin/documents` | 知识库 | houtai.html 表格 | ⚠️ 硬编码 + **批量删除** | ✅ |
| `/admin/agents` | Agent 监控 | houtai.html 卡片 | ⚠️ 硬编码 | ✅ |

---

## 共享组件

### 学生端（`src/components/layout/`）

- **`Sidebar.tsx`** — 7 项菜单 + 用户信息 + 底部登出按钮
- **`Header.tsx`** — 60px 玻璃拟态 + 动态页面标题

### 管理端（`src/lib/admin/`）

- **`context.tsx`** — `AdminProvider` + `useAdmin` Hook
  - 管理 `isAuth` / `user` / `activePanel` / `login` / `logout` / `showToast`
  - localStorage 隔离 `zhishu_admin_token` / `zhishu_admin_user`
- **`components.tsx`** — 共享 UI
  - `AdminCheckbox` — 复选框（支持 `indeterminate` 半选）
  - `BatchDeleteBar` — 批量操作条
  - `useSelection(items)` — 多选状态管理 Hook

---

## API 客户端

### 学生端（`src/lib/api.ts`）

| 模块 | 端点数 | 自动带 token |
|------|--------|--------------|
| `authApi` | login / register / me | ✅ |
| `profileApi` | build / get | ✅ |
| `chatApi` | stream / getSessions / getMessages / deleteSession | ✅ |
| `resourceApi` | generate / generateStream / list | ✅ |
| `exerciseApi` | generate / generateStream / list | ✅ |
| `pathApi` | generate / generateStream / list / get | ✅ |
| `tutorApi` | ask | ✅ |
| `dashboardApi` | getStats / getCourses | ✅ |
| `evaluationApi` | recordAction / getStats / getReport | ✅ |

### 管理端（每个页面独立用 fetch）

- 调用 `http://localhost:8001/api/v1/...` 端点
- 用 `localStorage.getItem('zhishu_admin_token')` 拿 token
- 当前**用模板硬编码数据**（待后端实现 27 个 admin API）

---

## 关键设计决策

### 1. 布局隔离（NO_SHELL_ROUTES）
根 `app/layout.tsx` 维护 `NO_SHELL_ROUTES = ['/login', '/admin']`，让管理后台完全跳过学生端 Sidebar/Header，独立布局。

### 2. Token 隔离
- 学生端：`zhishu_token` / `zhishu_student` / `zhishu_refresh_token`
- 管理端：`zhishu_admin_token` / `zhishu_admin_user`
- 两套互不干扰，避免误操作

### 3. 批量删除模式
所有 6 个列表页统一模式：
```ts
const sel = useSelection(filtered)
function batchDelete() {
  if (sel.selectedCount === 0) return
  if (!confirm(`确认删除选中的 ${sel.selectedCount} 个 X？`)) return
  const ids = Array.from(sel.selected)
  setList(prev => prev.filter(x => !ids.includes(x.id)))
  sel.clear()
}
```

### 4. CSS 命名空间
- 学生端：`.card` / `.chat-main` / `.stat` / `.btn`
- 管理端：`.admin-cd` / `.admin-cb` / `.admin-batch-bar` / `.admin-btn`
- 用 `admin-` 前缀完全隔离，避免样式冲突

---

## 验证结果

| 验证项 | 结果 |
|--------|------|
| `npm run build` | ✅ 17 路由编译通过 |
| `npm run lint` | ✅ 0 errors（仅 2 个原有 warnings） |
| 学生端 8 页面 | ✅ 全部可访问 + 联调后端 |
| 管理后台 9 页面 | ✅ 全部可访问 + 模板硬编码数据 |
| 批量删除 6 页面 | ✅ 全选/反选/单选/半选 + 二次确认 |
| 登录注册 | ✅ JWT 认证 + 401 自动跳登录页 |

---

## 未完成（待办）

- 管理后台 27 个后端 API（设计文档：`docs/设计文档/管理后台设计文档.md`）
- 管理后台接入真实数据（替换硬编码）
- 操作日志（P2）
- 数据导出（P2）
- 13 处 `alert()` 替换为更好的 UI 反馈
- P1：`markdownToHtml` XSS 消毒
- P1：3 套 Resource / Exercise 类型定义统一
