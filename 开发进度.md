# 开发进度跟踪

> 最后更新：2026-06-08

## 总览

| 模块 | 状态 | 完成度 |
|------|------|--------|
| 前端 7 页面模板复刻 | ✅ 已完成 | 100% |
| 前端后端联调 | ⬜ 未开始 | 0% |
| 后端基础设施（FastAPI + 8 Model + 5 Router + 6 Agent） | ✅ 已完成 | 100% |
| F1 对话式画像 | ✅ 后端完成 | 90%（前端未联调） |
| F2 多智能体资源生成 | ⚠️ 缺 MindMap Agent | 80%（前端未联调） |
| F3 学习路径规划 | ✅ 后端完成 | 90%（前端未联调） |
| F4 智能辅导 | ⚠️ Agent 有，RAG 空 | 60%（前端未联调） |
| F5 效果评估 | ⬜ 未开始 | 0% |
| RAG + 防幻觉 | ⬜ 未开始 | 0% |
| Celery 异步任务 | ⬜ 未开始 | 0% |
| MinIO 文件存储 | ⬜ 未开始 | 0% |
| 测试 | ⬜ 未开始 | 0% |
| 部署与交付 | ⬜ 未开始 | 0% |

---

## 已完成

### 前端（7 页面，模板驱动）

所有 7 个页面 1:1 复刻 D:\桌面\muban 中同名 HTML 模板，CSS 集中在 globals.css。**全部使用假数据，0 个 fetch 调用。**

| 路由 | 页面 | 模板 | `'use client'` | 后端联调 |
|------|------|------|----------------|----------|
| `/` | 仪表盘 | yibiaopan.html | ❌ Server Component | ❌ |
| `/duihua` | 智能对话 | duihua.html | ✅ | ❌ |
| `/profile` | 学习画像 | huaxiang.html | ✅ | ❌ |
| `/resources` | 资源中心 | zhiyuan.html | ✅ | ❌ |
| `/path` | 学习路径 | lujing.html | ✅ | ❌ |
| `/tiku` | 练习题库 | tiku.html | ✅ | ❌ |
| `/pinggu` | 学习评估 | pinggu.html | ✅ | ❌ |

**共享组件**：`components/layout/Sidebar.tsx` + `Header.tsx`
**未使用文件**：`types/index.ts`（TS 类型）、`stores/appStore.ts`（Zustand store）已准备但未接入任何页面

### 后端（8 表 + 6 Agent + 16 API 端点）

#### 数据库（8 张表，全部就位）

| 表名 | 模型文件 | 用途 | 外键 |
|------|----------|------|------|
| `students` | `student.py` | 学生账户 | - |
| `student_profiles` | `student_profile.py` | 6 维 JSONB 画像 + 版本控制 | - |
| `document_chunks` | `document_chunk.py` | RAG 文档分块（embedding 用 JSONB 占位） | - |
| `resources` | `resource.py` | 生成的学习资源 | - |
| `learning_paths` | `learning_path.py` | DAG 学习路径 + 每日计划 | - |
| `exercises` | `exercise.py` | 生成的练习题 | - |
| `chat_sessions` | `chat_session.py` | 聊天会话 | - |
| `chat_messages` | `chat_message.py` | 聊天消息 | - |

> 注：开发阶段去掉了外键约束（`ForeignKey`），student_id 仅为 UUID 类型。生产环境需加回。

#### 6 个 Agent（全部可用）

| Agent | 文件 | 功能 | LLM 调用 |
|-------|------|------|----------|
| Profile Agent | `agents/profile_agent.py` | 对话式 6 维画像提取 | ✅ MiniMax-M3 |
| Document Agent | `agents/document_agent.py` | 知识讲解 + 代码 + 音频脚本 | ✅ MiniMax-M3 |
| Exercise Agent | `agents/exercise_agent.py` | 自适应练习题生成 | ✅ MiniMax-M3 |
| Path Agent | `agents/path_agent.py` | 学习路径规划（DAG） | ✅ MiniMax-M3 |
| Tutor Agent | `agents/tutor_agent.py` | RAG 智能问答 | ✅ MiniMax-M3 |
| Master Agent | `agents/master_agent.py` | 多 Agent 编排器（LLM 路由） | ✅ MiniMax-M3 |
| ❌ MindMap Agent | - | 思维导图 Mermaid 生成 | **未实现** |

#### 16 个 API 端点（全部测试通过）

| 方法 | 路径 | 标签 | 状态 |
|------|------|------|------|
| GET | `/` | 根路由 | ✅ |
| GET | `/health` | 健康检查 | ✅ |
| POST | `/api/v1/profile/build` | 学习画像 | ✅ 对话→LLM→6 维 JSON→存库 |
| GET | `/api/v1/profile/{student_id}` | 学习画像 | ✅ 查询当前画像 |
| POST | `/api/v1/resource/generate` | 资源生成 | ✅ Document Agent 生成 |
| GET | `/api/v1/resource/list` | 资源生成 | ✅ 资源列表 |
| POST | `/api/v1/resource/exercises/generate` | 资源生成 | ✅ Exercise Agent 生成 |
| GET | `/api/v1/resource/exercises/{student_id}` | 资源生成 | ✅ 练习题列表 |
| POST | `/api/v1/path/generate` | 学习路径 | ✅ Path Agent 生成 |
| GET | `/api/v1/path/{student_id}` | 学习路径 | ✅ 路径列表 |
| GET | `/api/v1/path/{student_id}/{path_id}` | 学习路径 | ✅ 路径详情 |
| POST | `/api/v1/tutor/ask` | 智能辅导 | ✅（RAG 检索 TODO） |
| POST | `/api/v1/tutor/generate` | 智能辅导 | ✅ 资源生成（与 resource 重复） |
| POST | `/api/v1/chat/stream` | 聊天 | ✅ SSE 流式 + Master Agent 路由 |
| GET | `/api/v1/chat/sessions/{student_id}` | 聊天 | ✅ 会话列表 |
| GET | `/api/v1/chat/sessions/{session_id}/messages` | 聊天 | ✅ 消息历史 |

#### LLM 客户端

| 文件 | 功能 | 状态 |
|------|------|------|
| `services/minimax_client.py` | httpx 直接调用 MiniMax-M3（OpenAI 兼容格式） | ✅ 可用 |
| `services/minimax_langchain.py` | LangChain BaseChatModel 封装 | ✅ 可用 |

#### 配置

| 文件 | 状态 |
|------|------|
| `core/config.py` | ✅ MINIMAX_* 字段可用，SPARK_* 预留注释 |
| `core/database.py` | ✅ async SQLAlchemy + pgvector 可选 |
| `.env` | ✅ 含 MiniMax API Key（已 gitignore） |

---

## 未完成（按优先级排序）

### P0 — 前端联调（演示必须）

- [ ] 创建 `lib/api.ts` API 客户端
- [ ] 对话页 `/duihua` → 接 `chat/stream` SSE
- [ ] 画像页 `/profile` → 接 `profile/build` + `profile/{id}`
- [ ] 资源页 `/resources` → 接 `resource/generate` + `list`
- [ ] 路径页 `/path` → 接 `path/generate`
- [ ] 题库页 `/tiku` → 接 `exercises/generate`
- [ ] 评估页 `/pinggu` → 接 `tutor/ask`（生成评估报告）
- [ ] 仪表盘 `/` → 从其他接口聚合数据

### P0 — 后端补全

- [ ] **MindMap Agent** — 思维导图 Mermaid 生成（F2 评分项）
- [ ] **防幻觉三层** — 模式检测 + 来源验证 + LLM 语义校验（N3 必做）
- [ ] **所有生成接口支持 SSE 流式** — 资源/练习/路径生成都应流式返回

### P1 — RAG + 高级功能

- [ ] RAG 管道（文档解析 → 语义切片 → Embedding → pgvector 检索 → LLM 重排）
- [ ] F5 效果评估（行为跟踪 + 统计分析）
- [ ] Celery 异步任务 + Redis 缓存
- [ ] MinIO 文件存储

### P2 — 收尾

- [ ] 测试（Agent 单元测试 + API 集成测试）
- [ ] 部署与交付
- [ ] 讯飞星火 V4 切换（上线前）

---

## 已知问题

| # | 问题 | 状态 |
|---|------|------|
| 1 | pgvector 扩展未安装（Python 包已装，PostgreSQL 扩展未装） | ⚠️ 向量检索暂不可用，embedding 用 JSONB 占位 |
| 2 | `resources/page.tsx:313` 有 TS 错误（`r.type` 索引问题） | ⚠️ `npm run dev` 不影响，`npm run build` 会失败 |
| 3 | `echo=True` 在 database.py | ⚠️ 开发阶段可用，生产需关闭 |
| 4 | `tutor.py /generate` 与 `resource.py /generate` 重复 | 待清理 |
| 5 | 无 Docker 环境 | ⚠️ PostgreSQL/Redis 需本地安装 |

---

## 变更日志

| 日期 | 变更 |
|------|------|
| 2026-06-08 | 后端全面实现：8 表 + 6 Agent + 16 API 端点，全部测试通过；MiniMax-M3 端到端验证通过；去掉开发阶段外键约束 |
| 2026-06-07 | 前端 7 页面全部复刻完成（pinggu/tiku 新增）；Agent 架构精简为 6 个子 Agent |
| 2026-06-06 | 前端 5 页面模板复刻（profile/resources/path/duihua/dashboard） |
| 2026-06-05 | 后端骨架初始化：FastAPI + 3 Model + 5 Router + docker-compose |

---

## 数据库连接信息

- **PostgreSQL**: `postgresql+asyncpg://postgres:123456@localhost:5432/zhishu`（端口 5432，D:\2026test\PostgreSQL\18）
- **Redis**: `redis://localhost:6379/0`（D:\2026test\redis_server）
- **LLM**: MiniMax-M3，`https://api.minimax.chat/v1`（OpenAI 兼容格式）
