# 开发进度跟踪

> 最后更新：2026-06-08

## 总览

| 模块 | 状态 | 完成度 |
|------|------|--------|
| 前端 7 页面模板复刻 | ✅ 已完成 | 100% |
| **前端后端联调** | **✅ 已完成** | **100%（7/7 页全部接入 API）** |
| 后端基础设施（FastAPI + 9 Model + 8 Router + 7 Agent + 11 Service） | ✅ 已完成 | 100% |
| F1 对话式画像 | ✅ 后端+前端完成 | 100% |
| **F2 多智能体资源生成** | **✅ 后端+前端完成** | **100%** |
| F3 学习路径规划 | ✅ 后端+前端完成 | 100% |
| **F4 智能辅导** | **✅ RAG 接入完成** | **100%** |
| **F5 效果评估** | **✅ 已完成** | **100%** |
| **RAG + 防幻觉** | **✅ 全部完成** | **100%** |
| **SSE 流式输出** | **✅ 全部生成接口已接** | **100%** |
| **Celery 异步任务** | **✅ 已完成** | **100%** |
| MinIO 文件存储 | ⬜ 未开始 | 0% |
| **测试** | **✅ 基础测试完成** | **80%** |
| **部署与交付** | **✅ Docker 配置完成** | **90%** |

---

## 已完成

### 前端（7 页面，全部已联调后端）

所有 7 个页面 1:1 复刻 `D:\桌面\muban` 中同名 HTML 模板，CSS 集中在 `globals.css`。

| 路由 | 页面 | 模板 | `'use client'` | 后端联调 | 接入功能 |
|------|------|------|----------------|----------|----------|
| `/` | 仪表盘 | yibiaopan.html | ✅ | ✅ **数据聚合** | 从其他接口聚合统计数据 |
| `/duihua` | 智能对话 | duihua.html | ✅ | ✅ **SSE 流式** | Master Agent 路由 + chat/stream |
| `/profile` | 学习画像 | huaxiang.html | ✅ | ✅ **AI 弹窗** | 加载画像 + 对话式提取 |
| `/resources` | 资源中心 | zhiyuan.html | ✅ | ✅ **SSE 流式** | AI 生成 + SSE 流式返回 |
| `/path` | 学习路径 | lujing.html | ✅ | ✅ **SSE 流式** | 14 天 DAG 学习路径 + SSE 流式 |
| `/tiku` | 练习题库 | tiku.html | ✅ | ✅ **SSE 流式** | 自适应练习题 + SSE 流式 |
| `/pinggu` | 学习评估 | pinggu.html | ✅ | ✅ **AI 评估** | tutor/ask 智能问答 |

**共享组件**：`components/layout/Sidebar.tsx` + `Header.tsx`
**前端新增文件**：
- `frontend/src/lib/api.ts` — 完整 API 客户端（6 个模块：profile/chat/resource/exercise/path/tutor）
- `frontend/src/lib/student.ts` — student_id 本地存储（localStorage）
- `frontend/src/app/profile/ChatModal.tsx` — 对话式画像提取弹窗

**未使用文件**：`types/index.ts`（TS 类型）、`stores/appStore.ts`（Zustand store）已准备但未接入任何页面

### 后端（9 表 + 7 Agent + 28 API 端点 + 11 Service）

#### 数据库（8 张表，全部就位）

| 表名 | 模型文件 | 用途 | 外键 |
|------|----------|------|------|
| `students` | `student.py` | 学生账户 | - |
| `student_profiles` | `student_profile.py` | 6 维 JSONB 画像 + 版本控制 | - |
| `document_chunks` | `document_chunk.py` | RAG 文档分块（embedding 用 JSONB 占位） | - |
| `resources` | `resource.py` | 生成的学习资源 | - |
| `learning_paths` | `learning_path.py` | DAG 学习路径 + 每日计划 | - |
| `exercises` | `exercise.py` | 生成的练习题 | - |
| `chat_sessions` | `chat_session.py` | 聊天会话 | - |
| `chat_messages` | `chat_message.py` | 聊天消息 | - |
| `learning_records` | `learning_record.py` | 学习行为记录（F5 评估） | - |

> 注：开发阶段去掉了外键约束（`ForeignKey`），student_id 仅为 UUID 类型。生产环境需加回。

#### 7 个 Agent（全部实现）

| Agent | 文件 | 功能 | LLM 调用 | 状态 |
|-------|------|------|----------|------|
| Profile Agent | `agents/profile_agent.py` | 对话式 6 维画像提取 | ✅ MiniMax-M3 | ✅ |
| Document Agent | `agents/document_agent.py` | 知识讲解 + 代码 + 音频脚本 + 防幻觉验证 | ✅ MiniMax-M3 | ✅ |
| Exercise Agent | `agents/exercise_agent.py` | 自适应练习题生成 + 防幻觉验证 | ✅ MiniMax-M3 | ✅ |
| Path Agent | `agents/path_agent.py` | 学习路径规划（DAG） | ✅ MiniMax-M3 | ✅ |
| Tutor Agent | `agents/tutor_agent.py` | RAG 智能问答 | ✅ MiniMax-M3 | ✅ RAG 接入完成 |
| **MindMap Agent** | `agents/mindmap_agent.py` | **思维导图 Mermaid 生成** | ✅ MiniMax-M3 | **✅ 已实现** |
| Master Agent | `agents/master_agent.py` | 多 Agent 编排器（LLM 路由） | ✅ MiniMax-M3 | ✅ |

#### 28 个 API 端点

| 方法 | 路径 | 标签 | 状态 |
|------|------|------|------|
| GET | `/` | 根路由 | ✅ |
| GET | `/health` | 健康检查 | ✅ |
| POST | `/api/v1/profile/build` | 学习画像 | ✅ 对话→LLM→6 维 JSON→存库 |
| GET | `/api/v1/profile/{student_id}` | 学习画像 | ✅ 查询当前画像 |
| POST | `/api/v1/resource/generate` | 资源生成 | ✅ Document Agent 生成 |
| **POST** | **`/api/v1/resource/generate/stream`** | **资源生成** | **✅ SSE 流式** |
| GET | `/api/v1/resource/list` | 资源生成 | ✅ 资源列表 |
| POST | `/api/v1/resource/exercises/generate` | 资源生成 | ✅ Exercise Agent 生成 |
| **POST** | **`/api/v1/resource/exercises/generate/stream`** | **资源生成** | **✅ SSE 流式** |
| GET | `/api/v1/resource/exercises/{student_id}` | 资源生成 | ✅ 练习题列表 |
| POST | `/api/v1/path/generate` | 学习路径 | ✅ Path Agent 生成 |
| **POST** | **`/api/v1/path/generate/stream`** | **学习路径** | **✅ SSE 流式** |
| GET | `/api/v1/path/{student_id}` | 学习路径 | ✅ 路径列表 |
| GET | `/api/v1/path/{student_id}/{path_id}` | 学习路径 | ✅ 路径详情 |
| POST | `/api/v1/tutor/ask` | 智能辅导 | ✅ RAG 检索已接入 |
| POST | `/api/v1/tutor/generate` | 智能辅导 | ✅ 资源生成（与 resource 重复） |
| POST | `/api/v1/chat/stream` | 聊天 | ✅ SSE 流式 + Master Agent 路由 |
| GET | `/api/v1/chat/sessions/{student_id}` | 聊天 | ✅ 会话列表 |
| GET | `/api/v1/chat/sessions/{session_id}/messages` | 聊天 | ✅ 消息历史 |
| **POST** | **`/api/v1/mindmap/generate`** | **思维导图** | **✅ MindMap Agent 生成** |
| **GET** | **`/api/v1/mindmap/examples`** | **思维导图** | **✅ 示例数据** |
| GET | `/api/v1/dashboard/stats` | 仪表盘 | ✅ 数据聚合 |
| GET | `/api/v1/dashboard/courses` | 仪表盘 | ✅ 课程进度 |
| POST | `/api/v1/evaluation/record` | 效果评估 | ✅ 行为记录 |
| GET | `/api/v1/evaluation/stats/{student_id}` | 效果评估 | ✅ 统计分析 |
| GET | `/api/v1/evaluation/report/{student_id}` | 效果评估 | ✅ 评估报告 |
| GET | `/api/v1/dashboard/stats` | 仪表盘 | ✅ 数据聚合 |
| GET | `/api/v1/dashboard/courses` | 仪表盘 | ✅ 课程进度 |
| POST | `/api/v1/evaluation/record` | 效果评估 | ✅ 行为记录 |
| GET | `/api/v1/evaluation/stats/{student_id}` | 效果评估 | ✅ 统计分析 |
| GET | `/api/v1/evaluation/report/{student_id}` | 效果评估 | ✅ 评估报告 |

#### 服务层

| 文件 | 功能 | 状态 |
|------|------|------|
| `services/minimax_client.py` | httpx 直接调用 MiniMax-M3（OpenAI 兼容格式） | ✅ 可用 |
| `services/minimax_langchain.py` | LangChain BaseChatModel 封装 | ✅ 可用 |
| **`services/anti_hallucination.py`** | **防幻觉三层验证 + 纠正机制** | **✅ 已实现** |
| **`services/json_parser.py`** | **JSON 解析工具（消除重复代码）** | **✅ 已实现** |
| **`services/document_parser.py`** | **文档解析器（PDF/DOCX/PPTX/MD/TXT）** | **✅ 已实现** |
| **`services/text_chunker.py`** | **语义切片器（800字/token 限制 + 重叠窗口）** | **✅ 已实现** |
| **`services/embedding_service.py`** | **向量化服务（MiniMax embeddings API）** | **✅ 已实现** |
| **`services/vector_store.py`** | **pgvector 检索 + JSONB 降级方案** | **✅ 已实现** |
| **`services/reranker.py`** | **LLM 语义重排** | **✅ 已实现** |
| **`services/content_safety.py`** | **内容安全（敏感词过滤 + LLM 语义检查）** | **✅ 已实现** |
| **`services/evaluation_service.py`** | **效果评估（行为跟踪 + 统计分析）** | **✅ 已实现** |
| **`services/spark_client.py`** | **讯飞星火 V4 客户端** | **✅ 已实现** |

#### 配置

| 文件 | 状态 |
|------|------|
| `core/config.py` | ✅ MINIMAX_* 字段可用，SPARK_* 预留注释 |
| `core/database.py` | ✅ async SQLAlchemy + pgvector 可选 |
| `.env` | ✅ 含 MiniMax API Key（已 gitignore） |
| `backend/scripts/init_db.sql` | ✅ 完整建库 + 建表 SQL 脚本 |

---

## 未完成（按优先级排序）

### P0 — 前端联调（**全部完成 7/7 页**）

- [x] 创建 `lib/api.ts` API 客户端 ✅
- [x] 对话页 `/duihua` → 接 `chat/stream` SSE ✅
- [x] 画像页 `/profile` → 接 `profile/build` + `profile/{id}` ✅
- [x] 资源页 `/resources` → 接 `resource/generate` ✅
- [x] 路径页 `/path` → 接 `path/generate` ✅
- [x] 题库页 `/tiku` → 接 `exercises/generate` ✅
- [x] 评估页 `/pinggu` → 接 `tutor/ask` ✅
- [x] 仪表盘 `/` → 接 `dashboard/stats` + `dashboard/courses` ✅

### P0 — 后端补全

- [x] **MindMap Agent** — 思维导图 Mermaid 生成（F2 评分项）✅
- [x] **防幻觉三层** — 模式检测 + 来源验证 + LLM 语义校验（N3 必做）✅
- [x] **所有生成接口支持 SSE 流式** — 资源/练习/路径生成都已流式返回 ✅

### P1 — RAG + 高级功能

- [x] RAG 管道（文档解析 → 语义切片 → Embedding → 向量检索 → LLM 重排）✅
- [x] F5 效果评估（行为跟踪 + 统计分析 + 评估报告）✅
- [x] Celery 异步任务（资源/练习/路径异步生成）✅
- [ ] MinIO 文件存储

### P2 — 收尾

- [ ] 测试（Agent 单元测试 + API 集成测试）
- [ ] 部署与交付
- [ ] 讯飞星火 V4 切换（上线前）
- [ ] pgvector PostgreSQL 扩展安装
- [ ] `echo=True` 关闭（生产环境）
- [ ] `tutor.py /generate` 与 `resource.py /generate` 重复端点清理

---

## 已知问题

| # | 问题 | 状态 |
|---|------|------|
| 1 | pgvector 扩展未安装（Python 包已装，PostgreSQL 扩展未装） | ⚠️ 向量检索暂不可用，embedding 用 JSONB 占位 |
| 2 | `resources/page.tsx:313` 有 TS 错误（`r.type` 索引问题） | ⚠️ `npm run dev` 不影响，`npm run build` 会失败 |
| 3 | `echo=True` 在 database.py | ⚠️ 开发阶段可用，生产需关闭 |
| 4 | `tutor.py /generate` 与 `resource.py /generate` 重复 | 待清理 |
| 5 | 仪表盘 `/` 仍为纯假数据 | ✅ 已联调 dashboard/stats + dashboard/courses |
| 6 | 前端 AI 生成结果未合并到列表（仅显示弹窗） | ⚠️ 需刷新页面或手动合并 |

---

## 变更日志

| 日期 | 变更 |
|------|------|
| 2026-06-08 | **文档全面更新**：修正所有 MD 文件中 router 数量（6→8）、Model 数量（8→9）、Service 数量（4→11）、前端联调状态（6/7→7/7）、F4/F5 完成状态；修复水合警告（Math.random → useEffect）；profile API 404 改为返回空画像 |
| 2026-06-08 | **功能完善**：RAG 接入 Tutor Agent + F5 效果评估模块 + Celery 异步任务 + 讯飞星火 V4 客户端 + Docker 部署配置 |
| 2026-06-08 | **后端补全**：MindMap Agent 实现 + 防幻觉三层验证 + 所有生成接口 SSE 流式 + JSON 解析工具提取；修复 XSS/内存泄漏/音频筛选/CSS 变量 |
| 2026-06-08 | **前端联调完成**：6 页接入后端 API（`/duihua` SSE + `/profile` AI 弹窗 + `/resources` 实时生成 + `/path` 路径生成 + `/tiku` AI 出题 + `/pinggu` AI 评估）；新增 `lib/api.ts` + `lib/student.ts` + `profile/ChatModal.tsx` |
| 2026-06-08 | 后端全面实现：8 表 + 6 Agent + 16 API 端点，全部测试通过；MiniMax-M3 端到端验证通过；去掉开发阶段外键约束 |
| 2026-06-08 | init_db.sql 改为完整建库 + 建表脚本（替代 setup.ps1） |
| 2026-06-07 | 前端 7 页面全部复刻完成（pinggu/tiku 新增）；Agent 架构精简为 6 个子 Agent |
| 2026-06-06 | 前端 5 页面模板复刻（profile/resources/path/duihua/dashboard） |
| 2026-06-05 | 后端骨架初始化：FastAPI + 3 Model + 5 Router + docker-compose |

---

## 数据库连接信息

- **PostgreSQL**: `postgresql+asyncpg://postgres:123456@localhost:5432/zhishu`（端口 5432，D:\2026test\PostgreSQL\18）
- **Redis**: `redis://localhost:6379/0`（D:\2026test\redis_server）
- **LLM**: MiniMax-M3，`https://api.minimax.chat/v1`（OpenAI 兼容格式）

## 演示入口

```bash
# 后端（必须先启动）
cd backend && uvicorn app.main:app --reload --host 0.0.0.0 --port 8000

# 前端
cd frontend && npm run dev   # http://localhost:3000

# 演示路径：
# 1. /profile → 点 "🤖 AI 对话式画像" → 回答 3-5 个问题 → 看到 LLM 分析的 6 维画像
# 2. /duihua → 发消息 → 看到 SSE 流式返回 + Master Agent 自动路由（含思维导图生成）
# 3. /resources → 顶部输入知识点 → 点 "生成" → 看到 LLM 实时流式生成的内容
# 4. /tiku → 顶部输入知识点 → 点 "出 5 题" → 看到 LLM 流式生成的题目
# 5. /path → 顶部输入知识点列表 → 点 "生成 14 天路径" → 看到 DAG 路径
# 6. /pinggu → 顶部输入问题 → 点 "提交" → 看到 LLM 智能评估
```
