# 开发进度跟踪

> 最后更新：2026-06-27（评估报告 AI 化 + 学习评估页硬编码移除）

## 总览

| 模块 | 状态 | 完成度 |
|------|------|--------|
| 前端 9 学生页面模板复刻 | ✅ 已完成 | 100% |
| **前端后端联调** | **✅ 已完成** | **100%（9/9 页全部接入 API）** |
| **前端 9 管理后台页面** | **✅ 已完成** | **100%（含批量删除 + 题库管理 CRUD 接后端）** |
| 后端基础设施（FastAPI + 11 表 + 10 Router + 8 Agent + 13 Service + 39 API） | ✅ 已完成 | 100% |
| **Master Agent StateGraph 编排** | **✅ 已完成** | **100%（10 节点 StateGraph + 7 子 Agent 协同）** |
| **管理后台后端权限** | **✅ 已完成** | **100%（role/is_active/last_login + bcrypt 初始化脚本）** |
| **公共题库系统** | **✅ 已完成** | **100%（exercise_bank 表 + 管理员 6 CRUD + 学生端题池 API）** |
| **用户设置页面** | **✅ 已完成** | **100%（个人信息编辑 + 修改密码 + 账号信息展示）** |
| **对话→题库联动** | **✅ 已完成** | **100%（StateGraph exercise 保存 DB + 跳转链接 + ?kp= 自动聚焦）** |
| **题库页增强** | **✅ 已完成** | **100%（隐藏/清空 + AI出题数量可选 + 答案格式三层防护 + 去重限容）** |
| **资源中心重构** | **✅ 已完成** | **100%（移除假数据 + API 全量重构 + 收藏持久化 + 批量生成 + 预置资源）** |
| **智能对话增强** | **✅ 已完成** | **100%（右侧上下文 + 动态推荐 + 消息复制 + 资源持久化）** |
| **骨架屏 loading** | **✅ 已完成** | **100%（resources/path/tiku/profile 4 页面 shimmer 动画）** |
| **Robot 图标** | **✅ 已完成** | **100%（components/RobotIcon.tsx 极简 SVG，替换 🤖 emoji）** |
| **学习时长追踪** | **✅ 已完成** | **100%（usePageTimer hook + 5 页面自动上报 + 评估报告聚合）** |
| **评估报告 AI 化** | **✅ 已完成** | **100%（LLM 生成自然语言报告 + 7 天趋势对比 + 知识点掌握度统计 + 易错点分析 + 规则引擎降级）** |
| **F1 对话式画像** | **✅ 后端+前端完成** | **100%（5 维 + 评分逻辑优化 + 进度显示 + 重新评估 + 能力分析报告）** |
| **F2 多智能体资源生成** | **✅ 后端+前端完成** | **100%（dual-format 流式）** |
| F3 学习路径规划 | ✅ 后端+前端完成 | 100% |
| **F4 智能辅导** | **✅ RAG 接入完成** | **100%** |
| **F5 效果评估** | **✅ LLM 评估报告 + 趋势分析 + 规则引擎降级** | **100%**（知识点掌握度统计、7天趋势对比、LLM 自然语言报告、硬编码全部移除） |
| **RAG + 防幻觉** | **✅ 全部完成** | **100%** |
| **SSE 流式输出** | **✅ 全部生成接口已接** | **100%**（4 个真流式端点） |
| **登录注册系统** | **✅ 已完成** | **100%**（bcrypt + JWT + 36 业务端点门禁） |
| **多轮对话上下文** | **✅ 已完成** | **100%**（最近 10 条消息传给 LLM） |
| **会话删除** | **✅ 已完成** | **100%**（DELETE /sessions/{session_id} + 前端 × 按钮） |
| MinIO / Celery 异步任务 | ⬜ 未使用 | 0% |
| **单元测试** | **✅ 114 pytest 全过** | **100%**（7 文件，含 message_bus + state_graph + strip_think） |
| **端到端冒烟测试** | **✅ 9/9 API 200** | **100%**（五次验证：06-09 / 06-10×2 / 06-11 / 06-11 管理后台） |
| **全面审计 P0 修复** | **✅ 10/10 全部完成** | **100%** |
| **管理后台开发** | **✅ 9 页面 + 批量删除 + 权限基础设施 + 题库管理 CRUD** | **100%** |
| **部署与交付** | **⚠️ 后端本地裸跑，Dockerfile 已存在但未使用** | **70%** |

---

## 已完成

### 前端（9 学生页面 + 9 管理页面）

**学生端** 9 个页面 1:1 复刻 `D:\桌面\muban` 中同名 HTML 模板，CSS 集中在 `globals.css`（米色/墨黑/琥珀设计系统）。

| 路由 | 页面 | 模板 | 后端联调 | 接入功能 |
|------|------|------|----------|----------|
| `/` | 仪表盘 | yibiaopan.html | ✅ **数据聚合** | 从其他接口聚合统计数据 |
| `/login` | 登录/注册 | login.html | ✅ **JWT 认证** | 注册/登录/自动跳转 |
| `/duihua` | 智能对话 | duihua.html | ✅ **SSE 流式 + 功能增强** | Master Agent 路由 + chat/stream + 会话管理 + 右侧上下文（从画像提取）+ 动态推荐 + 消息复制 + 资源持久化 |
| `/profile` | 学习画像 | huaxiang.html | ✅ **AI 弹窗 + 真实数据 + 进度显示** | 5 维画像 + 对话式评估 + 评估进度 + 能力分析报告 + 重新评估 + 置信度展示 |
| `/resources` | 资源中心 | zhiyuan.html | ✅ **API 全量重构** | 从 API 获取（移除假数据）+ 资源类型正确映射 + 收藏持久化 + 批量生成 + 预置资源库（5 个） |
| `/path` | 学习路径 | lujing.html | ✅ **SSE 流式** | DAG 学习路径 + SSE 流式 |
| `/tiku` | 练习题库 | tiku.html | ✅ **题池 API** | 公共题库 + AI 生成题混合 + SSE 流式出题 |
| `/pinggu` | 学习评估 | pinggu.html | ✅ **AI 评估** | tutor/ask 智能问答 + 图表源自 evalReport |
| `/setting` | 用户设置 | 新建 | ✅ **Auth API** | 个人信息编辑 + 修改密码 + 账号信息展示 |

**管理后台** 9 个页面 1:1 复刻 `D:\桌面\muban\houtai.html` 模板。

| 路由 | 页面 | 模板 | 后端联调 | 接入功能 |
|------|------|------|----------|----------|
| `/admin/login` | 管理员登录 | houtai.html 登录卡片 | ✅ **JWT + role 校验** | admin/admin123 预填 |
| `/admin` | 仪表盘 | houtai.html 仪表盘 | ⚠️ 硬编码 | 4 统计卡 + 2 趋势图 + 活跃表 |
| `/admin/users` | 用户管理 | houtai.html 表格 | ⚠️ 硬编码 | **含批量删除** + 详情弹窗 |
| `/admin/resources` | 资源管理 | houtai.html 表格 | ⚠️ 硬编码 | **含批量删除** + 详情弹窗 |
| `/admin/exercises` | 题库管理 | houtai.html 表格 | ✅ **管理端 API** | 公共题库 CRUD + 筛选 + 批量导入 + 新增/编辑/删除 |
| `/admin/paths` | 学习路径 | houtai.html 表格 | ⚠️ 硬编码 | **含批量删除** + DAG 弹窗 |
| `/admin/chats` | 对话记录 | houtai.html 表格 | ⚠️ 硬编码 | **含批量删除** + 对话弹窗 |
| `/admin/documents` | 知识库 | houtai.html 表格 | ⚠️ 硬编码 | **含批量删除** |
| `/admin/agents` | Agent 监控 | houtai.html 卡片 | ⚠️ 硬编码 | 7 Agent + 调用统计 + 错误率 |

**共享组件**：
- 学生端：`components/layout/Sidebar.tsx`（Zustand 订阅，实时更新用户名）+ `Header.tsx`（无退出按钮，设置按钮跳转 /setting）
- 管理端：`admin/layout.tsx`（含 admin-sb 侧边栏 + admin-hd 顶部 + 底部登出按钮）
- 批量删除共享：`lib/admin/components.tsx`（`AdminCheckbox` + `BatchDeleteBar` + `useSelection` Hook）
- 状态管理：`lib/admin/context.tsx`（`AdminProvider` + `useAdmin`）+ `stores/appStore.ts`（Zustand，Sidebar 实时读取 student）

**前端核心文件**：
- `src/lib/api.ts` — 学生端 API 客户端（12 个模块，自动带 token，含收藏/批量生成/保存对话资源 API）
- `src/lib/student.ts` — student_id 本地存储（从 `zhishu_student` 读取）
- `src/lib/utils.ts` — escapeHtml / markdownToHtml / extractAnswer
- `src/lib/admin/context.tsx` — 管理后台共享状态
- `src/lib/admin/components.tsx` — 管理后台共享组件
- `src/app/profile/ChatModal.tsx` — 对话式画像提取弹窗
- `src/stores/appStore.ts` — Zustand store（Sidebar 实时读取 student）

**未使用文件**：`types/index.ts`（TS 类型）已准备但未接入任何页面（tiku 用本地类型定义）

### 后端（11 表 + 8 Agent + 33 唯一 API 端点 + 13 Service + 10 Router）

#### 数据库（11 张表，全部就位）

| 表名 | 模型文件 | 用途 | 备注 |
|------|----------|------|------|
| `students` | `student.py` | 学生/管理员 | 含 `role`/`is_active`/`last_login`/`password_hash`/`student_no` |
| `student_profiles` | `student_profile.py` | **5 维** JSONB 画像 | 理解力/记忆力/应用转化/想象力/专注力 + confidence |
| `document_chunks` | `document_chunk.py` | RAG 文档分块 | embedding 用 JSONB 占位 |
| `resources` | `resource.py` | 生成的学习资源 | 含 `is_favorited`（收藏）+ `is_preset`（预置资源）|
| `learning_paths` | `learning_path.py` | DAG 学习路径 + 每日计划 | - |
| `exercises` | `exercise.py` | 生成的练习题 | 含 `source` 字段区分 ai/bank |
| `exercise_bank` | `exercise_bank.py` | **公共题库**（管理员录入） | 无 student_id，3 索引 |
| `chat_sessions` | `chat_session.py` | 聊天会话 | - |
| `chat_messages` | `chat_message.py` | 聊天消息 | content 列已改 TEXT（原 VARCHAR(10000) 截断长回复）|
| `learning_records` | `learning_record.py` | 学习行为记录 | F5 评估 |
| `learning_activity_logs` | `learning_activity_log.py` | **学习行为日志**（wyy 独占） | 行为审计 |

> 开发阶段去掉了外键约束（`ForeignKey`），student_id 仅为 UUID 类型。生产环境建议加回。
>
> **2026-06-11 新增**：`students.role` (VARCHAR(20) DEFAULT 'student') + `students.is_active` (BOOLEAN DEFAULT true) + `students.last_login` (TIMESTAMP) + `idx_students_role` 索引
>
> **2026-06-11 新增**：`exercise_bank` 表（公共题库，13 字段）+ `exercises.source` 字段（VARCHAR(20) DEFAULT 'ai'）
>
> **公共题库设计**：管理员通过 `/admin/exercises` 管理公共题库（exercise_bank 表），学生端通过 `/resource/exercises/pool` 获取题池（exercise_bank + exercises 合并，随机抽题）。AI 生成的题存入 exercises 表（source='ai'），管理员录入的题存入 exercise_bank 表（source='admin'）。

#### 8 个 Agent（全部实现，11 种 IntentType）

| Agent | 文件 | 功能 | LLM 调用 | 状态 |
|-------|------|------|----------|------|
| **Initial Assessment Agent** | `agents/initial_assessment_agent.py` | **对话式 5 维画像评估**（理解力/记忆力/应用转化/想象力/专注力 + 客观置信度计算 + 异常值检测 + 回退机制 + 进度推送） | ✅ MiniMax-M3 | ✅ |
| Document Agent | `agents/document_agent.py` | 知识讲解 + 代码 + 音频脚本 + 防幻觉验证 | ✅ MiniMax-M3 | ✅ |
| Exercise Agent | `agents/exercise_agent.py` | 自适应练习题生成 + 防幻觉验证 | ✅ MiniMax-M3 | ✅ |
| Path Agent | `agents/path_agent.py` | 学习路径规划（DAG） | ✅ MiniMax-M3 | ✅ |
| Tutor Agent | `agents/tutor_agent.py` | RAG 智能问答 + 多轮上下文 | ✅ MiniMax-M3 | ✅ |
| MindMap Agent | `agents/mindmap_agent.py` | 思维导图 Mermaid 生成 | ✅ MiniMax-M3 | ✅ |
| Audio Agent | `agents/audio_agent.py` | 音频脚本生成 | ✅ MiniMax-M3 | ✅ |
| Master Agent | `agents/master_agent.py` | **多 Agent 编排器（LangGraph StateGraph 10 节点）** | ✅ MiniMax-M3 | ✅ |

> **StateGraph 节点**（`master_agent.py`）：`intent_recognition → task_planning → conditional_route → 5 个 Agent 节点（document/mindmap/exercise/path/tutor/audio）→ result_aggregation → response_generation`，共 10 个节点（不是 13）。
>
> **11 种 IntentType**（`state.py`）：profile / document / exercise / path / tutor / mindmap / audio / resource_generate / learn_and_practice / full_course / multi_chat

#### 39 个唯一 API 端点（10 Router）

| 方法 | 路径 | 标签 | 功能 |
|------|------|------|------|
| GET | `/` | 根路由 | ✅ API 状态 |
| GET | `/health` | 健康检查 | ✅ |
| POST | `/api/v1/auth/login` | 认证 | ✅ bcrypt 校验 + JWT + 记录 last_login + 校验 is_active |
| POST | `/api/v1/auth/register` | 认证 | ✅ bcrypt 存储 + JWT |
| GET | `/api/v1/auth/me` | 认证 | ✅ 获取当前用户（JWT） |
| PUT | `/api/v1/auth/me` | 认证 | ✅ 更新姓名/邮箱 |
| POST | `/api/v1/auth/change-password` | 认证 | ✅ 修改密码（验证旧密码） |
| GET | `/api/v1/auth/me/{student_id}` | 认证 | ✅ 按 ID 获取用户（仅自己） |
| POST | `/api/v1/profile/assess/stream` | 学习画像 | ✅ **SSE 流式 5 维评估（Initial Assessment Agent）+ 进度实时推送** |
| GET | `/api/v1/profile/me` | 学习画像 | ✅ 获取当前学生画像（含 confidence） |
| POST | `/api/v1/profile/reset` | 学习画像 | ✅ **重置画像，允许重新评估** |
| GET | `/api/v1/profile/assessment-status` | 学习画像 | ✅ **获取评估状态（用于恢复评估）** |
| PUT | `/api/v1/profile/background` | 学习画像 | ✅ **更新学习背景信息** |
| POST | `/api/v1/resource/generate` | 资源生成 | ✅ Document Agent 生成 |
| POST | `/api/v1/resource/generate/stream` | 资源生成 | ✅ **SSE 流式** |
| GET | `/api/v1/resource/list` | 资源生成 | ✅ 资源列表（含用户资源 + 预置资源，返回完整 content） |
| POST | `/api/v1/resource/exercises/generate` | 资源生成 | ✅ Exercise Agent 生成 |
| POST | `/api/v1/resource/exercises/generate/stream` | 资源生成 | ✅ **SSE 流式（dual-format）** |
| GET | `/api/v1/resource/exercises/{student_id}` | 资源生成 | ✅ 练习题列表 |
| GET | `/api/v1/resource/exercises/pool` | 资源生成 | ✅ **题池 API（公共题库 + AI 题合并，随机抽题）** |
| POST | `/api/v1/resource/{id}/favorite` | 资源生成 | ✅ **收藏/取消收藏资源** |
| POST | `/api/v1/resource/batch-generate` | 资源生成 | ✅ **批量生成多个知识点的资源** |
| POST | `/api/v1/resource/save-from-chat` | 资源生成 | ✅ **从对话页保存资源到资源中心** |
| POST | `/api/v1/path/generate` | 学习路径 | ✅ Path Agent 生成 |
| POST | `/api/v1/path/generate/stream` | 学习路径 | ✅ **SSE 流式** |
| GET | `/api/v1/path/{student_id}` | 学习路径 | ✅ 路径列表 |
| GET | `/api/v1/path/{student_id}/{path_id}` | 学习路径 | ✅ 路径详情 |
| POST | `/api/v1/tutor/ask` | 智能辅导 | ✅ RAG 检索已接入 |
| POST | `/api/v1/chat/stream` | 聊天 | ✅ **SSE 流式 + Master Agent 路由** |
| GET | `/api/v1/chat/sessions/{student_id}` | 聊天 | ✅ 列出会话 |
| GET | `/api/v1/chat/sessions/{session_id}/messages` | 聊天 | ✅ 获取消息历史 |
| DELETE | `/api/v1/chat/sessions/{session_id}` | 聊天 | ✅ 删除会话及所有消息 |
| POST | `/api/v1/mindmap/generate` | 思维导图 | ✅ MindMap Agent |
| GET | `/api/v1/mindmap/examples` | 思维导图 | ✅ 示例数据（公开） |
| GET | `/api/v1/dashboard/stats` | 仪表盘 | ✅ 数据聚合 |
| GET | `/api/v1/dashboard/courses` | 仪表盘 | ✅ 课程进度 |
| POST | `/api/v1/evaluation/record` | 效果评估 | ✅ 行为记录 |
| GET | `/api/v1/evaluation/stats/{student_id}` | 效果评估 | ✅ 统计分析 |
| GET | `/api/v1/evaluation/report/{student_id}` | 效果评估 | ✅ 评估报告 |
| GET | `/api/v1/admin/exercises` | 管理端-题库 | ✅ 分页查询（知识点/题型/难度/搜索筛选） |
| POST | `/api/v1/admin/exercises` | 管理端-题库 | ✅ 新增单题 |
| POST | `/api/v1/admin/exercises/batch` | 管理端-题库 | ✅ 批量导入（JSON 数组） |
| PUT | `/api/v1/admin/exercises/{id}` | 管理端-题库 | ✅ 修改题目 |
| DELETE | `/api/v1/admin/exercises/{id}` | 管理端-题库 | ✅ 删除题目 |
| GET | `/api/v1/admin/exercises/knowledge-points` | 管理端-题库 | ✅ 知识点列表（用于筛选下拉） |

> **管理后台设计文档 `docs/设计文档/管理后台设计文档.md` 中 27 个 API**：题库管理 6 个接口已实现，其余 21 个（用户/资源/路径/对话/文档/Agent 监控）前端用模板硬编码数据演示。

#### 服务层（13 个 Service）

| 文件 | 功能 | 状态 |
|------|------|------|
| `services/minimax_client.py` | httpx 直接调用 MiniMax-M3（OpenAI 兼容格式） | ✅ 可用 |
| `services/minimax_langchain.py` | LangChain BaseChatModel 封装 | ✅ 可用 |
| `services/spark_client.py` | 讯飞星火 V4 客户端（同步 + 流式） | ✅ 已实现 |
| `services/anti_hallucination.py` | 防幻觉三层验证（PatternDetector + SourceValidator + LLMValidator） | ✅ 已实现 |
| `services/json_parser.py` | JSON 解析工具 | ✅ 已实现 |
| `services/document_parser.py` | 文档解析器（PDF/DOCX/PPTX/MD/TXT） | ✅ 已实现 |
| `services/text_chunker.py` | 语义切片器（800字限制 + 重叠窗口） | ✅ 已实现 |
| `services/embedding_service.py` | 向量化服务（MiniMax embeddings API） | ✅ 已实现 |
| `services/vector_store.py` | pgvector 检索 + JSONB 降级方案 | ✅ 已实现 |
| `services/reranker.py` | LLM 语义重排 | ✅ 已实现 |
| `services/content_safety.py` | 内容安全（敏感词过滤 + LLM 语义检查） | ✅ 已实现 |
| `services/evaluation_service.py` | 效果评估（行为跟踪 + 统计分析） | ✅ 已实现 |
| `agents/communicator.py` | **MessageBus pub/sub 通信**（在 agents/ 目录但本质是 Service） | ✅ 已实现 |

#### 配置

| 文件 | 状态 |
|------|------|
| `core/config.py` | ✅ MINIMAX_* + SPARK_* + JWT_SECRET + LLM_PROVIDER 切换 |
| `core/database.py` | ✅ async SQLAlchemy + pgvector 可选 |
| `core/dependencies.py` | ✅ UUID 校验 + get_current_user 门禁 |
| `core/security.py` | ✅ bcrypt 密码哈希 + JWT 生成/验证 |
| `core/celery_config.py` | ⚠️ 已存在但未启动 worker |
| `backend/scripts/init_db.sql` | ✅ 完整建库 + 建表 SQL（11 表 + 14 索引 + admin 种子数据） |
| `backend/scripts/init_admin.py` | ✅ 自动 ALTER + bcrypt 哈希 + 创建/重置 admin 账号 |
| `backend/scripts/migrate_exercise_bank.sql` | ✅ exercise_bank 建表 + exercises.source 字段 |
| `backend/scripts/migrate_resource_favorite.sql` | ✅ resources 表添加 is_favorited 字段 |
| `backend/scripts/init_preset_resources.py` | ✅ 预置资源初始化（5 个人工智能导论课程资源） |
| `backend/scripts/run_migration.py` | ✅ 通用迁移执行器 |
| `.env` | ✅ 含 MiniMax API Key（已 gitignore） |

---

## 全部 10 个 P0 问题已修复（2026-06-10）

> 2026-06-10 全面审计发现 47+ 问题（P0×10 / P1×20+ / P2×15+），以下 P0 全部修完：

| # | 问题 | 修复方式 | 状态 |
|---|------|----------|------|
| 1 | `learning_records` 表不存在 | `init_db.sql` 新增 + `Base.metadata.create_all` | ✅ |
| 2 | `evaluationApi.recordAction` 从未调用 | 前端 5 处调用（resources/tiku/duihua/path） | ✅ |
| 3 | `/tutor/ask` 调 `embed_text` 不存在 | 改为 `embed_single` | ✅ |
| 4 | UUID 校验只在 mindmap.py | 新建 `core/dependencies.py` + 7 router Pydantic 验证 | ✅ |
| 5 | `Exercise.is_correct` 永远 None | 已被 P0-1+2 覆盖：recordAction 传 score | ✅ |
| 6 | SSE error 没跟 done | 实际全部已存在，误报 | ✅ |
| 7 | `_strip_think` 中途关闭吞内容 | 后续排查为误修（死代码），已回滚 | ✅ |
| 8 | `/profile` 5 个数组全 hardcoded | 改为从 `realProfile` 派生 | ✅ |
| 9 | `/resources` 10 张卡全 hardcoded | API 资源合并入网格渲染 | ✅ |
| 10 | `/pinggu` 6 个数组全 hardcoded | 图表改为 props 传入，数据来自 evalReport | ✅ |

### P1 关键问题（部分已修）

| # | 问题 | 状态 |
|---|------|------|
| 1 | `chat_message.content` 存 JSON 字符串 | ✅ **已修复**：前端加载 assistant 消息时自动解析 answer 字段 + content 列改 TEXT 类型 |
| 2 | 防幻觉正则太激进 | ⚠️ 待修 |
| 3 | 引用匹配逻辑错误（UUID vs "[1]"） | ⚠️ 待修 |
| 4 | `print()` 代替 logging（10+ 处） | ⚠️ 待修 |
| 5 | `markdownToHtml` 不消毒 → XSS 风险 6 处 | ⚠️ 待修 |
| 6 | 3 套 `Resource` 类型定义互相漂移 | ⚠️ 待修 |
| 7 | 3 套 `Exercise` 类型定义互相漂移 | ⚠️ tiku 页面已统一，api.ts 类型为准 |
| 8 | 13 处 `alert()` | ⚠️ 待修 |
| 9 | `appStore.ts` Zustand 0 引用 | ✅ Sidebar 已接入（实时更新用户名） |
| 10 | `chatApi.stream` 无超时/重试 | ⚠️ 待修 |
| 11 | `profile_agent` 调 LLM 报 `'HumanMessage' object is not subscriptable` | ✅ **已重写**为 `initial_assessment_agent`（wyy 合并 PR） |
| 12 | 管理后台 27 个 API 未实现 | ⚠️ 题库 6 个已实现，剩余 21 个待实现（前端用硬编码） |
| 13 | 资源中心静态假数据与 API 数据混合 | ✅ **已修复**：移除假数据，全部从 API 获取 |
| 14 | 资源中心 API 资源类型全部显示为"文档" | ✅ **已修复**：后端返回正确 resource_type + 前端类型推断 |
| 15 | 资源中心详情页无法查看 API 资源内容 | ✅ **已修复**：后端 /list 接口返回完整 content 字段 |
| 16 | 资源中心收藏功能不持久化 | ✅ **已修复**：新增 is_favorited 字段 + 收藏 API |
| 17 | 智能对话右侧上下文面板为空 | ✅ **已修复**：从 profile 提取知识点和薄弱环节 |
| 18 | 智能对话推荐问题静态 | ✅ **已修复**：根据画像薄弱环节动态生成 |

---

## 端到端冒烟测试

五次验证全部 9/9 PASS：

- **2026-06-09**（commit `433c8ba`）：首次冒烟，9 API 全 200
- **2026-06-10 17:29**（StateGraph 升级后）：第二次验证，多 Agent 协同真实发生
- **2026-06-10 20:30**（P0 修复后）：第三次验证，114 pytest 全过
- **2026-06-11**（登录注册系统后）：第四次验证，密码哈希 + JWT + 门禁机制
- **2026-06-11**（管理后台 + 角色权限）：第五次验证，admin 账号 + 批量删除 + 角色隔离

详情见 [`SMOKE_TEST_REPORT.md`](SMOKE_TEST_REPORT.md)。

---

## 数据库连接信息

- **PostgreSQL**: `postgresql+asyncpg://postgres:123456@localhost:5432/zhishu`（端口 5432）
- **Redis**: `redis://localhost:6379/0`（当前未实际使用）
- **LLM**: MiniMax-M3，`https://api.minimax.chat/v1`（OpenAI 兼容格式）
- **备选 LLM**: 讯飞星火 V4（改 `.env` 中 `LLM_PROVIDER=spark`）

## 端口约定

- **后端**: `http://localhost:8001`（**不要用 8000**：Windows 端口僵尸 socket 问题）
- **前端**: `http://localhost:3000`
- **前端 api.ts** 的 `BASE_URL` 已配 `http://localhost:8001/api/v1`

## 演示入口

```bash
# 后端（必须先启动，端口 8001）
cd backend && venv/Scripts/python -m uvicorn app.main:app --host 0.0.0.0 --port 8001

# 前端
cd frontend && npm run dev   # http://localhost:3000

# 学生端演示路径：
# 1. /login → 注册 → 登录 → JWT token 自动生效
# 2. /profile → 点 "开始评估" → 回答 3-5 个问题 → 看到评估进度（轮数+维度收集）→ 完成后看到 5 维画像 + 能力分析报告 + 置信度
# 2b. /profile → 点 "重新评估" → 确认重置 → 重新开始评估
# 3. /duihua → 发消息 → 看到 SSE 流式返回 + Master Agent 自动路由 + 右侧显示知识点/薄弱环节/资源列表
# 4. /resources → 看到 5 个预置资源 + 用户生成的资源 → 顶部输入知识点（逗号分隔可批量）→ 点 "生成"
# 5. /tiku → 看到题池（公共题库 + AI 生成题混合）→ 顶部输入知识点 → 选数量(5/10/15/20) → 点 "生成" → AI 流式生成新题
# 6. /tiku → 隐藏/清空功能：每道题右下角 ✕ 隐藏 → 侧边栏"已隐藏"卡片恢复 → "清空列表"批量隐藏
# 7. /duihua → 说"出5道算法题" → AI 回复题目 + 跳转链接 → 点击跳转到 /tiku?kp=算法 → 知识点自动填入
# 8. /path → 顶部输入知识点列表 → 点 "生成 14 天路径" → 看到 DAG 路径
# 9. /pinggu → 顶部输入问题 → 点 "提交" → 看到 LLM 智能评估
# 10. / → 仪表盘 → 看到刚才生成的数据聚合
# 11. /setting → 查看个人信息 → 修改姓名/邮箱 → 修改密码
# 12. 回到 /duihua → 删除旧会话 → 新开会话 → 证明会话管理
# 13. 回到 /duihua → 刷新页面 → 证明会话持久化（自动恢复上次对话）

# 管理后台演示路径（admin / admin123）：
# 1. /admin/login → 登录 → /admin 仪表盘
# 2. /admin/exercises → 题库管理 → 新增题目（选择题/判断题/简答题/编程题）
# 3. /admin/exercises → 批量导入 → 粘贴 JSON 数组 → 一键导入
# 4. /admin/users → 勾选多个用户 → 批量删除 → 二次确认
# 5. /admin/resources → 查看资源详情（弹窗）→ 删除
# 6. /admin/paths / admin/chats / admin/documents → 同样支持批量删除
# 7. /admin/agents → 查看 7 个 Agent 集群状态 + 调用统计 + 错误率
# 8. 侧边栏底部 → 点登出按钮 → 跳回 /admin/login
```

---

## 变更日志

| 日期 | 变更 |
|------|------|
| 2026-06-27 | **评估报告 AI 化 + 学习评估页硬编码移除**（8 项改动）：① 后端 evaluation_service.py 重写：新增 LLM 评估报告生成（`_generate_llm_report` 调 MiniMax/Spark 生成自然语言报告）② 新增趋势计算（`_calculate_trend`：近7天 vs 之前7天正确率/时长对比）③ 新增知识点掌握度统计（`_get_exercise_details`：按知识点统计正确/错误数）④ 新增规则引擎降级（LLM 失败时 fallback 到 `_generate_fallback_report`）⑤ 评估 API 返回 `report` + `profile` 字段 ⑥ 前端 pinggu/page.tsx 移除全部 20+ 条硬编码数据，改用 LLM 报告动态渲染 ⑦ 前端 api.ts EvaluationReport 接口新增 report/profile 字段 ⑧ 其他修复：path 页类型导入重构、duihua 页 TS 类型安全修复、resources 页 Set 兼容性修复 |
| 2026-06-27 | **骨架屏 loading + Robot 图标 + 学习时长追踪**（5 项改动）：① globals.css 新增 skeleton shimmer 动画 CSS ② resources/path/tiku/profile 4 页面替换文字 loading 为骨架屏 ③ 新增 components/RobotIcon.tsx 极简机器人 SVG 图标 ④ resources/tiku/pinggu 页面 🤖 emoji 替换为 RobotIcon ⑤ 新增 hooks/usePageTimer.ts 页面停留计时器，5 页面自动记录 duration_seconds → learning_records 表 ⑥ 修复 DB：resources 表添加 is_preset 列 + init_db.sql 同步 |
| 2026-06-15 晚 | **智能对话修复 + 学习计划功能完善**（20 项改动）：① 智能对话：exercise 意图修复（流式输出不显示答案，完成后显示题目列表+跳转链接）② 智能对话：document/mindmap 意图添加 `_strip_think` 去掉 LLM 思考标签 ③ 智能对话：会话加载时正确解析 exercise/document/mindmap 等消息格式 ④ 智能对话：内部链接在当前页面跳转 ⑤ 学习计划：名称从"学习路径"改为"学习计划" ⑥ 学习计划：添加天数选择器（7/14/21/30天）⑦ 学习计划：添加每日学习量设置（2/3/5/8个知识点）⑧ 学习计划：自动估算天数（根据知识点数量和每日学习量）⑨ 学习计划：添加删除计划功能（后端 DELETE API）⑩ 学习计划：添加重置按钮 ⑪ 学习计划：显示计划描述 + loading 动画 ⑫ 学习计划：后端 API 添加 daily_topics 参数 ⑬ 学习计划：添加标记节点状态功能（完成/进行中）⑭ 学习计划：添加推荐学习顺序（根据依赖关系推荐下一个知识点）⑮ 学习计划：添加分享功能（复制链接到剪贴板）⑯ 学习计划：添加导出功能（导出为TXT文件）⑰ 学习计划：完善统计卡片（总体进度/已完成/薄弱项/预计时长）⑱ 学习计划：优化输入框样式（可输入+下拉选择组合框）⑲ 学习计划：移除数字输入框调节按钮 ⑳ 学习计划：输入框支持清空为空 |
| 2026-06-15 | **资源中心全面重构 + 智能对话功能增强**（15 项改动）：① 资源中心：移除静态假数据，全部从 API 获取 ② 后端 /resource/list 返回完整数据（含 content/difficulty/is_favorited）③ Resource 模型新增 is_favorited + is_preset 字段 ④ 新增收藏 API POST /resource/{id}/favorite ⑤ 新增批量生成 API POST /resource/batch-generate（支持逗号分隔多个知识点）⑥ 新增保存对话资源 API POST /resource/save-from-chat ⑦ 预置资源库：5 个人工智能导论课程核心资源（A*搜索/机器学习/深度学习/知识表示/Python代码）⑧ 前端资源中心重写：正确映射资源类型和内容、收藏持久化、生成后自动刷新列表、批量生成支持 ⑨ 智能对话：右侧上下文面板从 profile 提取知识点和薄弱环节（不再为空）⑩ 动态推荐问题：根据画像薄弱环节自动生成推荐 ⑪ 消息复制按钮：每条 AI 回复下方添加复制功能 ⑫ 资源持久化：生成 document/mindmap 时自动保存到 DB ⑬ 右侧资源面板从 DB 读取最近 10 个资源 ⑭ 后端 StateGraph 伪流式改善：执行过程中推送详细进度 + token 切片从 16 改为 8 字符更平滑 ⑮ 数据库迁移脚本：migrate_resource_favorite.sql + init_preset_resources.py |
| 2026-06-16 | **学习画像评分逻辑优化 + 前端 Profile 页面重构**（12 项改动）：① 后端评分逻辑：添加详细评分标准到 System Prompt + 客观置信度计算函数 `_calculate_objective_confidence`（基于回答长度/例子/反思/关键词）+ 异常值检测 `_validate_score`（分数变化不超过 25 分）+ 严格完成条件 `_check_completion`（至少 3 轮 + 至少 3 维度有效）+ 回退机制（LLM 不返回 ASSESS_DATA 时使用默认分数）② 后端新增 3 个 API：POST /profile/reset（重置画像）+ GET /profile/assessment-status（评估状态）+ PUT /profile/background（背景信息）③ 前端 Profile 页面重构：自定义 ConfirmDialog + AlertDialog 组件（现代化设计，适配米色/墨黑/琥珀配色）+ 能力分析报告组件 AnalysisReport（综合评分/最强最弱维度/提升建议）+ 评估进度显示（轮数+维度收集进度圆点指示器）+ 继续评估功能 + 重新评估按钮 + 置信度展示 + 页面布局调整（分析报告移到各维度详解后面）④ 合并 main 分支（解决 6 个文件冲突）⑤ 全面测试验证通过 |
| 2026-06-15 | **对话页刷新修复**（3 项改动）：① 对话页 sessionId 持久化 localStorage（组件挂载自动恢复上次会话）② loadSession 渲染修复（rendered 统一 false → markdownToHtml 始终渲染历史消息 + strip think 标签残留 + JSON 解析失败兜底）③ 后端 chat_messages.content 列 VARCHAR(10000) → TEXT（避免长回复截断导致 JSON 解析失败）|
| 2026-06-15 | **对话→题库联动**（6 项改动）：① 对话页 StateGraph exercise 意图保存到 exercises 表（含去重+限容20道）② 回复末尾追加跳转链接 `[👉 点击前往题库作答](/tiku?kp=xxx)` ③ 题库页读取 `?kp=` 参数自动填入知识点 ④ 题库页隐藏/清空系统（localStorage + 侧边栏恢复 + ConfirmDialog）⑤ AI 出题数量可选（分段选择器 5/10/15/20，后端支持 1-50）⑥ 答案格式三层防护（Prompt规范+后端normalize_answer+前端parseAnswer容错）⑦ 题库去重+限容（difflib相似度>0.85判重，每知识点最多20道AI题）⑧ 对话历史JSON解析（前端加载assistant消息时自动解析answer字段）⑨ 首页dashboard student_id修复 |
| 2026-06-14 | **MD 全量同步**（8 文件）：① 端点 39 → 33（实际唯一 endpoint 数）② 节点 13 → 10（master_agent.py 实际节点）③ 画像 6 维 → 5 维（wyy 重构）④ 测试 119 → 114（按文件逐个核对）⑤ 端口 8000 → 8001 ⑥ 加 `learning_activity_log.py` 模型 ⑦ 加 `initial_assessment_agent.py` Agent ⑧ 加 `run_migration.py` 脚本 ⑨ 加 `run_migration.py` 文档 ⑩ 更新 `SMOKE_TEST_REPORT.md` 标记 |
| 2026-06-14 | **画像评估修复**：① 系统提示花括号转义错误（`{{{{`→`{{`）LLM 误解导致 JSON 解析失败 ② 兜底分数机制（LLM 没返回有效分数时按轮次估算）③ chart.js 依赖补装 + BASE_URL 8000→8001 ④ 后端重启 |
| 2026-06-13 | wyy 合并：5 维画像重构 + Chart.js 雷达图 + `initial_assessment_agent` 替换 `profile_agent` + 前端 profile 页面重设计 + `LearningActivityLog` 模型 |
| 2026-06-13 | **公共题库 + 设置页面**（6 项大改动）：① 公共题库系统（`exercise_bank` 表 + 管理员 CRUD 5 接口 + 学生端题池 API `/resource/exercises/pool`，admin 题 + AI 题混合随机抽题）② 管理端题库管理页面（`/admin/exercises`：表格 + 筛选 + 分页 + 新增/编辑弹窗 + 批量导入 JSON + 删除）③ 学生端 tiku 页面完全重写（去掉 10 道硬编码题，改从题池 API 加载）④ 用户设置页面（`/setting`：`auth.py` 重写 GET/PUT /me + POST /change-password，前端新建 setting/page.tsx）⑤ Header 去掉退出按钮（模板设计），设置按钮加 onClick 跳转 ⑥ P1 修复：Exercise 类型漂移（tiku 统一为 api.ts 类型）、appStore Zustand 已接入 Sidebar |
| 2026-06-11 | **管理后台完成**（4 项大改动）：① 登录注册系统 + 多轮对话 + 会话删除（bcrypt + JWT + 全路由门禁 + SSE stream auth + final_state 累积 bug 修复 + 关键词路由扩展）② 前端 9 管理页面 1:1 复刻 houtai.html 模板（含批量删除、详情弹窗、DAG 可视化、Agent 监控）③ 后端权限基础设施（students 表加 role/is_active/last_login 字段、StudentDTO 返回 role、/auth/login 检查 is_active、init_admin.py Python 脚本）④ 学生端布局与 /admin 隔离（NO_SHELL_ROUTES 加 /admin）+ 统一登出样式 |
| 2026-06-10 20:30 | **P0 全部修复完成**：learning_records 建表 + recordAction 5 处调用 + UUID 校验依赖 + embed_text 拼写修复 + profile/resources/pinggu 3 页面硬编码替换 + `_strip_think` 死代码回滚 |
| 2026-06-10 18:30 | **全面代码审计**：发现 47+ 问题（P0×10 / P1×20+ / P2×15+） |
| 2026-06-10 17:29 | **第二次端到端冒烟测试**（StateGraph 升级后）：9/9 API PASS |
| 2026-06-09 | **StateGraph 升级完成**：Master Agent 从 if-else 路由改为 10 节点 LangGraph StateGraph |
| 2026-06-09 | **端到端冒烟测试入库**（commit `433c8ba`） |
| 2026-06-09 | **练习题 dual-format 流式修复** |
| 2026-06-09 | **全量审计并更新所有 MD 文件** |
| 2026-06-08 | **RAG 接入 Tutor Agent + F5 效果评估 + 防幻觉三层 + MindMap Agent** |
| 2026-06-08 | **前端联调完成**：8/8 页面全部接入后端 API |
| 2026-06-08 | 后端全面实现：9 表 + 8 Agent + 27 API 端点 + 12 Service |
| 2026-06-07 | 前端 7 页面全部复刻完成 |
| 2026-06-05 | 后端骨架初始化 |
